# 풀이

1.

# 코드

function solution(n, arr1, arr2) {

    let editedDouble
    let sum = []

    arr1.forEach(e => {
        let double = Number(e).toString(2)
        if( 5 - double.length ){
            for( let i = 0; i < (5 - double.length); i++){
                double = '0'.concat('', double)
            }
        }
        sum.push(double)
    })
    console.log(sum)
    // console.log(arr1[0].toString(2))

}

```js

```
